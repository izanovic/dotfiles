# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# Windows key
set $mod Mod4
# Alt key
set $mod2 Mod1
# Alt key
set $mod3 Mod3

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango: System San Francisco Display 9

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec alacritty #i3-sensible-terminal

# kill focused window
bindsym $mod+shift+q kill

# Color scheme
set $bg-color 	         #2b2b2b
set $inactive-bg-color   #2b2b2b
set $text-color          #FFC66D
set $inactive-text-color #A9B7C6
set $urgent-bg-color     #CB772F
set $preview-colour      #FFC66D

# Arrow keys alternative
set $up j	
set $left h
set $down k
set $right l

# start dmeu (a program launcher)
bindsym $mod+d exec dmenu_run -nb '#333333' -sb '#E95420'
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
# bindsym $mod+h split h

# split in vertical|horizontal orientation
bindsym $mod+v split toggle

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+e layout toggle split
# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
# bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+x focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $wsm1  "Laptop"
# set $wsm2  "second"
# set $wsm3  "third"

set $ws1  "1"
set $ws2  "2"
set $ws3  "3"
set $ws4  "4"
set $ws5  "5"
set $ws6  "6"
set $ws7  "7"
set $ws8  "8"
set $ws9  "9"
set $ws10 "10"

# set $ews1  "n1."
# set $ews2  "n2."
# set $ews3  "n3."
# set $ews4  "n4."
# set $ews5  "n5."
# set $ews6  "n6."
# set $ews7  "n7."
# set $ews8  "n8."
# set $ews9  "n9."
# set $ews10 "n10."

# Multi monitor setup
set $MON2 ePD-1-1 # Lenovo Laptop
set $MON1 HDMI-1-1 # 32" Monitor AOC
set $MON3 DP-1

#Set multimonitor setup
#bindsym $mod+$mod2+m exec --no-startup-id xrandr --output HDMI-1 --size 1920x1080 --pos 0x0 --rotate left --output DP-1 --size 1920x1080 --pos 1080x0 --rotate right --output eDP-1 --size 1920x1080 --pos 2160x800 --rotate normal --primary

bindsym $mod+$mod2+m exec --no-startup-id xrandr --output HDMI-1 --size 2560x1440 --pos 0x0 --rotate normal --output eDP-1 --size 1920x1080 --pos 2560x1000 --rotate normal --primary
bindsym $mod+$mod2+n exec --no-startup-id xrandr --output eDP-1-1 --size 1920x1080 --pos 0x0 --rotate normal --primary --output HDMI-1-1 --size 2560x1440 --pos 1920x0 --rotate normal 

# output of workpace
workspace $wsm1 output $MON2
# workspace $wsm2 output $MON2
# workspace $wsm3 output $MON2

workspace $ws1  output $MON1
workspace $ws2  output $MON1
workspace $ws3  output $MON1
workspace $ws4  output $MON1
workspace $ws5	output $MON1
workspace $ws6	output $MON1
workspace $ws7	output $MON1
workspace $ws8  output $MON1
workspace $ws9  output $MON1
workspace $ws10 output $MON1

# workspace $ews1  output $MON2
# workspace $ews2  output $MON2
# workspace $ews3  output $MON2
# workspace $ews4  output $MON2
# workspace $ews5	 output $MON2
# workspace $ews6	 output $MON2
# workspace $ews7	 output $MON2
# workspace $ews8  output $MON2
# workspace $ews9  output $MON2
# workspace $ews10 output $MON2

# move workspace to different output/screen
bindsym $mod+m move workspace to output left

# switch to workspace
# grave == `
bindsym $mod+grave workspace $wsm1
# bindsym $mod+minus workspace $wsm2
# bindsym $mod+equal workspace $wsm3

bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# bindsym $mod+$mod2+1 workspace $ews1
# bindsym $mod+$mod2+2 workspace $ews2
# bindsym $mod+$mod2+3 workspace $ews3
# bindsym $mod+$mod2+4 workspace $ews4
# bindsym $mod+$mod2+5 workspace $ews5
# bindsym $mod+$mod2+6 workspace $ews6
# bindsym $mod+$mod2+7 workspace $ews7
# bindsym $mod+$mod2+8 workspace $ews8
# bindsym $mod+$mod2+9 workspace $ews9
# bindsym $mod+$mod2+0 workspace $ews10

# move focused container to workspace

bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10
bindsym $mod+Shift+grave move container to workspace $wsm1

# bindsym $mod+$mod2+Shift+1 move container to workspace $ews1
# bindsym $mod+$mod2+Shift+2 move container to workspace $ews2
# bindsym $mod+$mod2+Shift+3 move container to workspace $ews3
# bindsym $mod+$mod2+Shift+4 move container to workspace $ews4
# bindsym $mod+$mod2+Shift+5 move container to workspace $ews5
# bindsym $mod+$mod2+Shift+6 move container to workspace $ews6
# bindsym $mod+$mod2+Shift+7 move container to workspace $ews7
# bindsym $mod+$mod2+Shift+8 move container to workspace $ews8
# bindsym $mod+$mod2+Shift+9 move container to workspace $ews9
# bindsym $mod+$mod2+Shift+0 move container to workspace $ews10

#workspace_auto_back_and_forth yes

# programs
bindsym $mod+$mod2+b exec --no-startup-id google-chrome --start-fullscreen
bindsym $mod+b exec --no-startup-id i3-msg 'exec firefox --start-fullscreen'
bindsym $mod+$mod2+f	exec --no-startup-id nautilus
bindsym $mod+o	exec "sh -c 'cd ~/oni2 && esy run'"

# Startup programming programs
bindsym $mod+p exec --no-startup-id i3-msg wokspace $ws4; exec firefox; split horizontal; exec "sh -c 'cd ~/oni2 && esy run'"; split vertical; exec alacritty; split horizontal; exec alacritty 
# -c 'cd ~/projects/LTO/LTO.UI.SeasonalWork' 

# take a screenshot
bindsym Print exec maim -s --format=png /dev/stdout | xclip -selection clipboard -t image/png -i

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

set $Locker  i3lock-fancy -g -- scrot -z && sleep 1

mode "mode_system" {
    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $Locker && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"  

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+x mode "default"
}

bindsym $mod+x mode "mode_system"

# window colors
#                       border              background         text                 indicator          child_border
client.focused          $urgent-bg-color    $bg-color          $text-color          $bg-color          #6897BB
client.unfocused        $incative-bg-color  $inactive-bg-color $inactive-text-color $inactive-bg-color
client.focused_inactive $acative-bg-color   $inactive-bg-color $inactive-text-color $inactive-bg-color
client.urgent           $urgent-bg-color    $urgent-bg-color   $text-color          $inactive-bg-color

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	position top
        status_command i3blocks
	font xft:Cousine, Fira Code 10, System San Francisco Display 9
	colors {
		background $bg-color
	    	separator #757575
		#                  border             background         text
		focused_workspace  $bg-color          $bg-color          $text-color
		inactive_workspace $inactive-bg-color $inactive-bg-color $inactive-text-color
		urgent_workspace   $inactive-bg-color $inacrive-bg-color $text-color
	}
}

########### Custom Binds ############

# Easier Rezising

set $resize 3

# Pressing left will shrink the window’s width.
# Pressing right will grow the window’s width.
# Pressing up will shrink the window’s height.
# Pressing down will grow the window’s height.
bindsym $mod+$mod2+$left resize shrink width $resize px or $resize ppt
bindsym $mod+$mod2+$up resize grow height $resize px or $resize ppt
bindsym $mod+$mod2+$down resize shrink height $resize px or $resize ppt
bindsym $mod+$mod2+$right resize grow width $resize px or $resize ppt

# same bindings, but for the arrow keys
bindsym $mod+$mod2+Left resize shrink width $resize px or $resize ppt
bindsym $mod+$mod2+Down resize grow height $resize px or $resize ppt
bindsym $mod+$mod2+Up resize shrink height $resize px or $resize ppt
bindsym $mod+$mod2+Right resize grow width $resize px or $resize ppt

# lockscreen
#bindsym $mod+x exec
exec --no-startup-id compton --config ~/.config/compton.conf -b

# Let specific windows float
for_window [window_role="pop-up"]                     floating enable,move absolute center
for_window [window_role="task_dialog"]                floating enable,move absolute center
for_window [title="Preferences$"]                     floating enable,move absolute center
for_window [class="Dia" window_role="toolbox_window"] floating enable,move absolute right,resize grow up 50 px or 50 ppt
for_window [title="Microsoft Teams Notification"]     floating enable,move absolute right,resize grow up 50 px or 50 ppt



for_window [title="Microsoft Teams Notification"]     move workspace 

# change volume or toggle mute
bindsym XF86AudioRaiseVolume exec amixer -q -D pulse sset Master 5%+ && pkill -RTMIN+10 i3blocks 
bindsym XF86AudioLowerVolume exec amixer -q -D pulse sset Master 5%- && pkill -RTMIN+10 i3blocks
bindsym XF86AudioMute exec amixer -q -D pulse sset Master toggle && pkill -RTMIN+10 i3blocks

# Sreen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness

# Touchpad controls
bindsym XF86TouchpadToggle exec xinput set-prop NUMOFDEV "Device Enabled" 0

# Dirty fix for pink background in terminal
exec Alacritty
 
# Media player controls
bindsym XF86AudioPlay exec playerctl play
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# Remove window borders
new_window pixel 1

# Set background
exec_always --no-startup-id feh --bg-scale /home/izanovic/Pictures/wallpaper.jpg

#startup

#bindsym $mod+$mod2+r exec --no-startup-id i3-msg workspace $ws1; exec google-chrome && sleep 1; workspace $ws8; exec slack

exec --no-startup-id clip
exec "setxkbmap -option ctrl:nocaps"

